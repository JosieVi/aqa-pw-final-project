# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: src/api/tests/orders/markProductAsReceived.spec.ts >> [API] [Orders] Products Receipt - Canceled Orders >> Should return error when trying to receive products for Canceled order - 400 Bad Request
- Location: src/api/tests/orders/markProductAsReceived.spec.ts:186:7

# Error details

```
Error: Expected status: 400, but got: 200

expect(received).toBe(expected) // Object.is equality

Expected: 400
Received: 200
```

```
Error: Expected isSuccess: false, but got: true

expect(received).toBe(expected) // Object.is equality

Expected: false
Received: true
```

```
Error: Expected errorMessage: Invalid order status, but got: null

expect(received).toBe(expected) // Object.is equality

Expected: "Invalid order status"
Received: null
```

# Test source

```ts
  1  | import { expect, test } from '@playwright/test';
  2  | import { IResponse, IResponseFields } from 'types/api.types';
  3  | 
  4  | export function validateResponse<T extends IResponseFields | null>(
  5  |   response: IResponse<T>,
  6  |   status: number,
  7  |   isSuccess?: boolean | null,
  8  |   errorMessage?: string | null,
  9  | ) {
  10 |   return test.step(`Validate response status: ${status}, isSuccess: ${isSuccess}, errorMessage: ${errorMessage}`, () => {
  11 |     expect.soft(response.status, `Expected status: ${status}, but got: ${response.status}`).toBe(status);
  12 | 
  13 |     const actualIsSuccess = response.body ? response.body.IsSuccess : undefined;
  14 |     const actualErrorMessage = response.body ? response.body.ErrorMessage : undefined;
  15 | 
  16 |     expect.soft(actualIsSuccess, `Expected isSuccess: ${isSuccess}, but got: ${actualIsSuccess}`).toBe(isSuccess);
  17 | 
> 18 |     expect.soft(actualErrorMessage, `Expected errorMessage: ${errorMessage}, but got: ${actualErrorMessage}`).toBe(errorMessage);
     |                                                                                                               ^ Error: Expected errorMessage: Invalid order status, but got: null
  19 |   });
  20 | }
  21 | 
```